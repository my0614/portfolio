# 예상 기술 면접 질문 — 김민영

포트폴리오/이력서에 실린 프로젝트와 기술 스택을 기준으로 나올 수 있는 기술 질문과, 실제 면접에서 답변할 수 있도록 정리한 답변안입니다.

> 답변은 포트폴리오/이력서에 기재된 사실을 기준으로 작성했고, 구체적인 수치·세부 구현 중 이력서에 명시되지 않은 부분은 실제 기억과 다를 수 있으니 면접 전 본인 기억으로 다시 검증해 주세요.

---

## 1. 사내 MLOps 플랫폼 DFLOW

### 아키텍처 / 설계

**Q. GOD·MMDetection·MMYOLO 3개 프레임워크를 "단일 ML 백엔드 인터페이스"로 추상화했다고 했는데, 구체적으로 인터페이스의 공통 메서드(예: `train`, `predict`, `export`)를 어떻게 설계했나요? 프레임워크마다 입출력 텐서 shape·config 포맷이 다른데 어디서 흡수했나요?**
A. 공통 추상 클래스에 `train(config)`, `predict(image)`, `export(format)` 세 메서드를 필수로 구현하도록 강제했습니다. 프레임워크별 config 포맷(MMDetection의 Config 객체, YOLO의 yaml, GOD 자체 dict) 차이는 각 프레임워크 어댑터 내부의 translator가 공통 스키마(모델명·데이터셋 경로·epoch·batch size·lr·pretrained 여부)로 매핑해 흡수했고, 출력 shape 차이는 raw output을 공통 Detection 결과 포맷(bbox·score·class_id)으로 바꾸는 post-processing 어댑터에서 흡수했습니다.
B. GOD·MMDetection·MMYOLO 각각 쿠버네티스의 파드로 올려서 ml 백앤드를 관리했습니다. GOD ml 백애드에는 여러개의 모델 등 각 ml 백앤드 모델 인터페이스를 통합한 구조입니다. 라벨툴 화면에서 학습을 진행하고 학습하기 이벤트를 주게되면 라벨링 했던 데이터들이 제가 만든 ml백앤드로 데이터가 넘어오는 구조로 입니다. 모델의 데이터와 학습할 모델 형식, 기본적인 파라미터들이 사용자에게서 내려왔고, 그 정보를 맞아서 선택한 학습 모델의 포맷 형식에 맞도록 라벨데이터셋을 만들어서 학습을 진행시켰습니다. yolo는 x,y,w,h 구조의 txt 파일은 faster-rcnn은 x,y,x,y 구조의 xml 구조들도 되었고
prediction 은 결과는 라벨링 데이터가 들어오는 동일한 인터페이스의 구조로 그대로 반환하게 됩니다. dflow는 auto-labeling 기능도 있기때문에
라벨링하는 툴에서 prediction 결과를 그대로 보여줘야 하는 상황도 잇기에 라벨링툴 인터페이스에 맞춰서 진행해주었습니다. export 는 크게 pytorch 기반 pt 파일과 onnx 모델 두가지를 다운받을 수 있도록 만들었습니다. onnx 프레임워크를 사용하여 onnx로 weights 변환후에 dflow 웹에서 바로 사용자가 다운받을 수 있도록 했고요, 현재 weights 가 어떤 정보를 가지고 학습했는지도 알려주기 위해 info.json 으로 파라미터, 선택모델, 버전등 과함께 주었습니다 나중에는 
onnx, pt 를 사용하여 바로 사용할 수있도록 docker-compose.yml 이나 환경 셋팅 정보들도 함께 넘겨주도록 고도화 작업까지 완료했습니다. god 같은 경우는 사용자가 DFlow에서 학습을 요청하면 모델 종류와 함께 Epoch, Batch Size, Learning Rate 등 학습 파라미터가 ML Backend로 전달됩니다.
GOD ML Backend에서는 전달받은 사용자 설정을 기반으로 학습이 시작되기 전에 각 모델이 사용하는 설정 파일을 동적으로 생성하거나 수정하도록 구현했습니다.
YOLO 계열은 모델별 yaml 또는 cfg 파일을 수정하여 데이터셋 경로, 클래스 수, 학습 파라미터 등을 반영했습니다.
MMDetection·MMYOLO 등 OpenMMLab 프레임워크는 모두 Python 기반의 config 파일을 사용하는 구조이기 때문에, 사용자가 선택한 모델과 학습 옵션에 맞게 필요한 설정만 동적으로 변경한 후 학습을 실행했습니다. 이를 통해 사용자는 복잡한 프레임워크별 설정 파일을 직접 수정할 필요 없이, 웹 화면에서 모델과 학습 옵션만 선택하면 각 프레임워크에 맞는 Config가 자동으로 생성되어 학습이 진행되는 환경을 구축했습니다.

**Q. 신규 모델을 "config 교체만으로" 온보딩할 수 있게 했다고 했는데, config 스키마는 어떻게 버저닝하고 검증(validation)했나요? 잘못된 config가 들어왔을 때 실패 지점은 어디인가요?**
A. config는 JSON Schema로 필수 필드와 파라미터 범위를 정의해 API 진입 시점에 1차 검증했고, 프레임워크별 config 파일 생성 후 실행 직전에 해당 프레임워크 파서로 2차 파싱 테스트를 거쳐 여기서 실패하면 큐에 올리지 않고 즉시 사용자에게 에러를 반환했습니다. 학습 job마다 사용된 config 스냅샷을 저장해 어떤 학습이 어떤 config로 돌았는지 추적할 수 있게 했습니다.
B. 저희는 사용자가 Config 파일을 직접 수정하는 구조는 아니었습니다. MMDetection이나 MMYOLO의 Config 파일은 모델 구조, Optimizer, Scheduler, Data Pipeline 등 설정이 매우 복잡하기 때문에, 사용자가 직접 수정하도록 하면 오류가 발생할 가능성이 높다고 판단했습니다.

그래서 각 모델별로 검증된 기본 Config를 템플릿으로 관리하고, 사용자가 선택한 모델과 하이퍼파라미터(Epoch, Batch Size, Learning Rate 등)처럼 변경이 필요한 일부 항목만 Backend에서 동적으로 수정하도록 구현했습니다.

또한 사용자가 학습을 요청할 때 선택한 모델 정보와 하이퍼파라미터는 train_job 테이블에 함께 저장하여 어떤 설정으로 학습했는지 버전 이력을 관리했습니다. 덕분에 이전 학습 설정을 그대로 재사용하거나 추적할 수 있었습니다.

Validation 측면에서는 사용자가 Config 파일 자체를 업로드하거나 수정하는 방식이 아니었기 때문에, 잘못된 Config가 생성될 가능성을 구조적으로 줄였습니다. Backend에서 모델과 데이터셋, 필수 파라미터를 확인한 뒤 검증된 템플릿의 필요한 값만 변경하여 학습을 시작하도록 했습니다.

실제 운영 과정에서도 Config 오류로 인해 학습이 실패한 사례는 없었습니다. 오히려 가장 많이 고민했던 부분은 Config를 어떻게 수정할 것인가보다 사용자에게 어느 수준까지 하이퍼파라미터를 노출할 것인가였습니다. 너무 많은 설정을 제공하면 사용성이 떨어지고 잘못된 설정으로 학습 품질이 저하될 수 있기 때문에, 실제로 자주 사용하는 핵심 하이퍼파라미터만 수정할 수 있도록 제한하고 나머지는 검증된 기본값을 유지하는 방향으로 설계했습니다.
**Q. 왜 자체 ML 백엔드(GOD)를 별도로 만들었나요? MMDetection/MMYOLO만으로 커버가 안 됐던 이유는 무엇인가요?**
A. MMDetection/MMYOLO는 특정 아키텍처 계열에 최적화돼 있어 설정 복잡도가 높고 프레임워크 업데이트에 종속되는 문제가 있었습니다. GOD은 사내에서 자주 쓰는 핵심 모델(YOLO·Faster R-CNN·RetinaNet)만 경량으로 직접 구현해, 로깅·체크포인트 포맷 등을 우리 플랫폼에 맞게 자유롭게 커스터마이징하고 외부 프레임워크 변경에 흔들리지 않도록 하기 위함이었습니다.
B. 처음 흐름은 GOD를 만들고 GOD ml 백앤드의 한계로 인해서 도입한게 MM프레임워크들 입니다. GOD은 사내에서 자주 쓰는 핵심 모델(YOLO·Faster R-CNN·RetinaNet)만 경량으로 직접 구현해, 로깅·체크포인트 포맷 등을 우리 플랫폼에 맞게 자유롭게 커스터마이징하고 외부 프레임워크 변경에 흔들리지 않도록 하기 위함이었습니다. 하지만 새로운 모델을 붙일때마다 cuda, pytorch 등 버전을 통합하여 할 수있는 환경을 만드는것에 한계를 느꼇고, 매번 통합 인터페이스에 맞춰서 다시 개발하는 것 또한 굉장히 비효율적이라는 것을 느끼게 되어서 모델마다 통합된 config 파일을 사용하여 MMdetection 과 MMyolo 프레임워크로 넘어오게 되었습니다. 

**Q. 이 추상화 레이어가 신규 프레임워크(예: 4번째 프레임워크) 추가에도 확장 가능한 구조인가요, 아니면 3개에 맞춰 하드코딩된 부분이 있나요?**
A. 어댑터 패턴으로 설계해 신규 프레임워크 추가 시 공통 인터페이스를 구현하는 어댑터 클래스만 추가하면 되는 구조였습니다. 다만 config 자동 생성 로직 일부는 3개 프레임워크의 공통 파라미터 셋을 기준으로 만들어져 있어, 크게 다른 파라미터 체계를 쓰는 프레임워크가 추가된다면 그 부분은 별도 작업이 필요했을 것이라 완전히 범용적이었다고는 말씀드리기 어렵습니다.


완전히 추상화된 구조는 아니었습니다. 당시에는 GOD, MMDetection, MMYOLO 세 가지를 대상으로 설계했기 때문에 모델 종류에 따라 분기하는 로직은 일부 존재했습니다. 다만 사용자 인터페이스는 공통으로 유지하고, Backend에서 선택한 모델에 따라 Config를 생성하고 적절한 학습 Backend로 전달하는 구조로 구현했습니다. 그래서 새로운 모델를 같은 프레임워크 내에 추가하는 것은 Config와 Weight만 추가하면 되었지만, 새로운 학습 프레임워크를 도입하는 경우에는 해당 프레임워크와 연동하는 로직을 추가로 개발해야 했습니다. Detectron2나 Ultralytics처럼 또 다른 프레임워크를 지원해야 한다면, 현재 구조에서도 연동은 가능하지만 일부 분기 로직을 수정해야 합니다. 그래서 프로젝트를 진행하면서는 다음 단계에서는 Adapter나 Plugin 형태로 프레임워크별 인터페이스를 분리하면 확장성이 더 좋아지겠다는 점을 느꼈습니다.
### Redis 큐 / 동시성

**Q. 기존 "DB 폴링" 방식에서 어떤 race condition이 있었는지 구체적으로 설명해 주세요.**
A. 여러 워커가 "PENDING 상태 job을 SELECT한 뒤 RUNNING으로 UPDATE"하는 방식이었는데, SELECT와 UPDATE 사이에 원자성이 없어 두 워커가 동시에 같은 job을 읽어가 중복 실행되는 문제가 있었습니다.

**Q. Redis BRPOP이 원자적으로 race condition을 없앤다고 했는데, BRPOP 자체의 한계(컨슈머가 pop 후 죽으면 작업 유실)는 어떻게 처리했나요?**
A. pop 직후 Redis Hash에 heartbeat 필드를 갱신하도록 하고, 별도 모니터링 프로세스가 일정 시간 heartbeat이 갱신되지 않는 job을 감지해 큐에 재적재하는 방식으로 유실을 방지했습니다.

**Q. Redis Hash로 job 상태를 관리한다고 했는데, Redis가 다운되면 학습 큐 전체가 멈추는 구조 아닌가요? 이 SPOF는 어떻게 완화했나요?**
A. 맞습니다, Redis는 이 아키텍처의 SPOF였습니다. AOF/RDB 영속화를 켜서 재시작 시 상태를 복구할 수 있게 했고, 실제 장애 빈도가 낮아 별도 이중화 없이도 수용 가능한 리스크로 판단했습니다. 다만 가장 아쉬운 지점으로, 향후 Sentinel/Replica 도입을 개선 과제로 남겨뒀습니다.

**Q. 큐에 우선순위나 fair-scheduling 개념이 있었나요, 아니면 FIFO였나요?**
A. 기본은 FIFO였습니다. 특정 사용자가 다량의 job을 등록하면 다른 사용자가 오래 대기하는 문제는 있었지만, 팀 규모의 실사용 패턴에서는 크게 이슈가 되지 않아 fair-share 스케줄러는 만들지 않고 향후 확장 과제로 남겼습니다.

### GPU 자원 관리

**Q. nvidia-smi 기반 동적 분배에서 확인 시점과 할당 시점 사이의 TOCTOU 문제는 없었나요?**
A. 말씀하신 TOCTOU(Time Of Check To Time Of Use) 문제는 GPU 스케줄링에서 충분히 발생할 수 있는 이슈입니다. 저희도 nvidia-smi로 GPU 사용 가능 여부를 조회한 뒤 학습을 실행하는 구조였기 때문에, 조회 시점과 실제 프로세스를 실행하는 시점 사이에 다른 프로세스가 동일한 GPU를 선점할 가능성은 이론적으로 존재했습니다. 다만 당시 시스템은 Redis FIFO 큐를 기반으로 학습 Job을 순차적으로 처리했고, 동시에 여러 학습 Job을 실행하는 구조가 아니었기 때문에 실제 운영에서는 동일 GPU를 두 Job이 동시에 선택하는 상황은 발생하지 않았습니다. 하지만 구조적으로는 완전한 해결책은 아니라고 생각합니다. 그래서 이후에는 단순히 유휴 GPU를 찾는 방식이 아니라 Job별 예상 GPU 메모리 사용량과 현재 GPU의 여유 메모리를 함께 고려하는 Resource-aware Scheduling을 검토했습니다. 더 나아가 GPU를 먼저 예약한 뒤 학습을 시작하는 Reservation 또는 Lock 메커니즘을 적용하면 TOCTOU 가능성도 크게 줄일 수 있다고 생각합니다.

**Q. Aliyun GPUShare로 전환했는데, nvidia-smi 방식과 비교해 무엇을 해결했고 MIG와는 어떻게 다른가요?**
A. nvidia-smi 방식은 애플리케이션 레벨에서 매번 여유 메모리를 조회해 판단하는 임시방편이라 동시성·정확도 이슈가 있었던 반면, GPUShare는 K8s 스케줄러 레벨에서 GPU를 fraction 단위 리소스로 선언해 스케줄링 자체가 경합을 보장합니다. MIG는 하드웨어 레벨 물리적 파티셔닝이라 격리는 강력하지만 지원 GPU가 제한적이고 파티션 크기가 고정적인 반면, GPUShare는 소프트웨어 레벨 공유라 더 유연합니다.

**Q. `min_gpu_memory`는 어떻게 산정했나요? 실제 사용량이 초과해 OOM이 나는 경우는 없었나요?**
A. 그 당시에는 산정하고 방어하는 기능은 없었고요, 만약에 지금이라도 하게 된다면 모델·배치 크기 조합별로 실제 학습을 돌려 nvidia-smi로 관측한 peak 메모리에 10~20% 여유 마진을 더해 산정할거같습니다.

### 자동 라벨링 / 데이터 플라이휠

**Q. 모델 confidence가 낮은 예측도 그대로 pre-label로 넣나요? confirmation bias 위험은 어떻게 방지했나요?**
A. confidence threshold 이상인 예측만 pre-label로 자동 생성하고 이하는 라벨을 비워둬 작업자가 직접 그리도록 했습니다. 또한 모든 pre-label은 사람이 검수·수정한 뒤에만 최종 데이터셋에 반영되도록 강제해, 검증 없이 모델의 실수가 그대로 누적되는 것을 막았습니다.
B. auto-labeling을 진행후에 결과값은 이미지와 결과값을 함께 반환하며 라벨링툴 위에 맵핑되게 됩니다. 아무리 학습이 잘되었다고 한들 auto-labeling은 사람이 한 라벨이 아니기 때문에 정답이 될 수 없어서 한번 사람이 확인하고 검수하도록 되어있고요, confidenece는 라벨링툴 위에서 원하는 confidence 이상의 값만 보여줄수있도록 하는 부분이 있어서 선택하여 최종 label 값으로 지정할 수 있습니다. 

**Q. "라벨링-학습 선순환 루프"에서 사람의 검수 단계가 빠지면 안 되는 이유는?**
A. 검수가 빠지면 모델의 편향(특정 클래스 과다 탐지 등)이 그대로 정답 데이터로 굳어져 다음 학습에서 같은 실수를 강화하는 악순환이 생기기 때문입니다. 실제로 검수 없이 자동 반영되는 경로는 두지 않았습니다.

### 성능/비교/배포

**Q. Precision·Recall·mAP는 어느 IoU threshold에서 계산했나요?**
A. 일반적인 관행에 따라 IoU 0.5를 기본으로 사용했고, 더 엄격한 기준이 필요한 프로젝트는 mAP@0.5:0.95도 함께 제공했습니다.
B. map, maP0.5 , 0.75 이렇게 3가지를 제공했습니다.

**Q. ONNX 변환 시 NMS 등 커스텀 레이어 호환성 문제는 없었나요?**
A. NMS처럼 프레임워크 자체 커스텀 연산은 ONNX 표준 연산자로 바로 변환되지 않아, ONNX가 지원하는 연산 조합으로 재구현하거나 NMS를 export 이후 별도 후처리 단계로 분리해 그래프 밖에서 수행하도록 처리했습니다.

**Q. K8s에서 학습 job은 Deployment/Job/StatefulSet 중 무엇으로 구성했나요? 파드 재시작 시 학습은 어떻게 이어지나요?**
A. 학습 job은 1회성 실행이라 K8s Job으로, 상시 대기하는 API 서버·워커는 Deployment로 구성했습니다. 학습 중 파드가 재시작될 경우를 대비해 주기적으로 체크포인트를 스토리지에 저장해, 재시작 시 마지막 체크포인트부터 이어서 재개할 수 있게 했습니다.
B. 파드는 전부 depolyment 로 실행했고요, 쿠버네티스의 Yaml 파일에 파드가 죽으면 바로 재실행할 수 있는 형태로 구조화 해놨습니다.
학습 job 은 ml 백앤드 파드 내부에서 redis job 으로 이벤트 처리하여 구성되어있습니다.
---

## 2. 드론 탑재 실시간 객체 탐지

### 모델 선정

**Q. 4종 모델을 동일 조건에서 비교했나요? 공정성을 위해 무엇을 통제했나요?**
A. 동일한 학습/검증 데이터 분할과 동일 입력 해상도, 유사한 epoch 수로 비교했습니다. 다만 모델별 권장 하이퍼파라미터(anchor 설정 등)는 각 모델이 최선의 성능을 낼 수 있는 조건에서 비교하는 것을 목표로 개별 적용했습니다.
B. 4종 모델 모두 동일한 조건으로 진행했습니다. 동일한 이미지와 데이터셋 개수, epoch 등 다만 모델별 권장 하이퍼파라미터(anchor 설정 등)는 각 모델이 최선의 성능을 낼 수 있는 조건에서 비교하는 것을 목표로 개별 적용했습니다. 또한 모델 성능이 좋아도 내가 원하는 객체 성능은 다를 수 있기 때문에 영상 자체에도 객체 탐지가 되어야 하는 이미지들도 선정하여 진행했습니다,

**Q. RetinaNet(Precision 96.8%, ~17FPS)이 탈락한 실시간 임계치 기준은 무엇인가요?**
A. 드론 짐벌의 영상 전송 프레임레이트와 현장 대응에 필요한 최소 반응성을 기준으로 약 10FPS 이상을 실사용 하한선으로 잡았습니다. RetinaNet의 17FPS는 수치상 충족하는 듯 보이지만, 실제 엣지 디바이스에서 Depth 처리·통신 등 다른 파이프라인 부하까지 얹었을 때 체감 지연이 커 실사용 기준에서 제외했습니다.
B. retinanet과 faster-rcnn 모델중에 결국 faster-rcnn으로 선택한 이유는 객체 크기에 대한 이슈였습니다,
faster-rcnn 모델을 각각 다른 객체의 크기의 차이가 크더라도 더 잘잡는 특징을 가지고 있기 때문입니다.

**Q. "Precision 최우선" 기준의 Recall 트레이드오프는 어떻게 합의됐나요?**
A. Recall이 다소 낮아지는(일부 위협 객체를 놓치는) 리스크를 감수했는데, 오탐 시 현장 대응팀이 잘못된 위치에 투입되는 것이 미탐보다 치명적이라는 점을 고객(보안기관)과 협의해 정한 기준이었습니다. Recall 저하분은 이후 데이터 보강·재학습으로 지속 개선했습니다.
B. 민군겸용 프로젝트 같은 경우는 특성상 오탐이나 일부 위협 객체를 놓치는 건 굉장히 치명적인 

### RGB/Depth 동기화

**새로**
A. 라이브러리 기본값으로는 RGB/Depth 퍼블리시 주기 차이 때문에 프레임 매칭 누락이 발생해, 실측 로그로 두 토픽의 발행 간격을 측정한 뒤 slop을 조금 더 넉넉하게, queue size는 버퍼 여유를 늘리는 방향으로 조정했습니다. slop을 너무 크게 잡으면 시간차가 큰 프레임끼리 짝지어져 좌표 정합 오차가 커지는 부작용이 있어, 매칭 성공률과 정합 오차 사이 균형점을 실측으로 찾았습니다.

**Q. 카메라 캘리브레이션은 언제/어떻게 검증했고, 렌즈 왜곡 보정은 별도로 했나요?**
A. RealSense D435i의 출고 시 intrinsic 값을 SDK에서 받아 사용했고, 체커보드 캘리브레이션으로 실측 거리와 산출 거리를 비교 검증했습니다. 렌즈 왜곡은 SDK가 제공하는 undistort 파라미터로 보정했습니다.

**Q. RGB/Depth의 FOV·해상도가 다를 경우 픽셀 좌표 매핑은 어떻게 정합시켰나요?**
A. RealSense SDK의 align 기능으로 Depth를 RGB 좌표계에 정렬해 픽셀 단위로 매핑을 정합시켰습니다.
B. RealSense 카메라 launch 파일에 align 파라미터를 추가하여 rostopic을 align된 이미지를 가져와서 정합시켰습니다. 
### 데이터 / 도메인 갭

**Q. synthetic-to-real 도메인 갭은 어떻게 완화했나요? domain randomization을 썼나요?**
A. 합성 이미지의 조명 각도·색상을 다양하게 변주하는 방식으로 domain randomization과 유사하게 처리했고, 완전한 프레임워크화보다는 실사 데이터 비중을 항상 더 높게 유지하고 합성 데이터는 보조적 역할로 한정하는 방식으로 갭을 관리했습니다.

**Q. 합성 데이터 비율과 실제 기여도는 어느 정도로 판단했나요(ablation)?**
A. 폭발물처럼 실사 확보가 어려운 클래스는 합성+증강 데이터가 상당 비중을 차지했습니다. 전 클래스 정식 ablation은 못했지만, 합성 데이터 추가 전후 해당 클래스의 Precision/Recall 변화를 비교해 개선을 확인한 뒤 반영했습니다.

**Q. 빛번짐을 background 클래스로 넣은 것과 hard negative mining의 차이는?**
A. background 클래스 학습은 hard negative mining의 한 형태라고 이해합니다. 모델이 실제로 자주 틀리는 패턴(빛번짐을 객체로 오인)을 정답 없는 이미지로 학습에 투입해, 해당 패턴을 배경으로 인식하도록 유도한 것입니다.
B. hard negtive mining에 포함되는 형태중 한개라고 생각합니다. 모델이 실제로 객체로 잘못 탐지하는 배경들을 정답이 없는 이미지로 학습 데이터셋에 넣는 것이기 때문입니다. 

**Q. injured_person(Recall 60.4%) 개선 결과는?**
A. Occlusion·저조도 데이터 보강 후 재학습에서 Recall이 개선됐고, 이는 데이터셋을 2,000장에서 6,000~7,000장으로 확장하며 전체 mAP가 61%→75%로 오른 결과에 기여한 주요 요인 중 하나였습니다.
B. 일반 보행자와 부상자의 특징에 대한 학습이 정말로 어려웠습니다. 사실 부상자역시 보행자와 비슷한 특징을 가지고 있었기 때문입니다. 과제 특성상 앉아있거나
누워있는 사람을 부상자로 간주하여 학습을 진행했고요, 다른 객체에비해 더 많은 학습 데이터셋을 추가적으로 넣었고, 다양한 환경과 자세, 행동들에 대한 이미지를 추가적으로 학습하여 성능을 개선했습니다.

### 시스템/네트워크

**Q. 서브넷 기반 환경 자동 판별에서 대역이 겹치는 예외 상황은 어떻게 방지했나요?**
A. 실환경(192.168.100.x)과 테스트 환경의 IP 대역을 운영 규칙으로 사전에 명확히 분리해 실무적으로 회피했습니다. 정교한 예외 처리보다 IP 대역 관리라는 운영 규칙으로 리스크를 통제한 것입니다.
B. 실제환경은 통신장비와 드론과 함께 사용하는 대역으로 새롭게 만든것이기 때문 대역이 겹치는 부분은 다행이 없었습니다. 하지만 실제환경과 테스트 환경 또는 드론 IP가 변경될때 접근ip,  ros 환경의 master_ip등을 모두 변경해주어야 했기 때문에 불편함과 비효율적인 부분이 있었는데요 이것은 ros에서 프로세스를 실행할때 자동으로 설정하도록 설계하였습니다.

**Q. ROS_MASTER_URI 런타임 주입 구조에서 멀티 마스터/네트워크 파티션 안정성은?**
A. 단일 드론-단일 GCS 페어로 운용되는 시나리오라 멀티 마스터 동시 운용은 설계 범위 밖이었고, 네트워크 단절 시에는 GCS의 재시작 명령 수신 시 ROS_MASTER_URI를 재주입하는 방식으로 복구했습니다.
B. 해당 시스템은 기본적으로 1대의 드론과 단일 GCS(Ground Control Station)를 기준으로 설계되어 있어 멀티 마스터 구조를 지원하지 않았습니다. 하지만 실제 운용 환경에서는 여러 대의 통신 드론을 번갈아 사용해야 하는 상황이 자주 발생했습니다.
이를 해결하기 위해 GCS를 별도의 스레드에서 실행하고, 객체 탐지 기능은 독립적인 서브프로세스로 분리하는 구조를 설계했습니다. 드론이 변경되면 GCS 스레드에 재시작 명령을 전달하여 새로운 드론과 자동으로 재연결하도록 했고, 연결이 완료되면 객체 탐지 프로세스도 새로운 연결 정보를 기반으로 다시 실행되도록 구성했습니다.
이를 통해 드론 교체 시 시스템 전체를 재시작하지 않고도 자동으로 새로운 드론과 연결되어 객체 탐지를 이어갈 수 있는 구조를 구현했으며, 실제 운용 중단 시간을 최소화할 수 있었습니다.

---

## 3. EO/IR UAV 실시간 탐지·추적 시스템

**Q. EO·IR 단일 데이터셋 학습 시 "새·항공기 오탐"의 근본 원인은?**
A. IR 영상은 저해상도·저대비 특성 때문에 새·항공기의 열화상 실루엣이 UAV와 유사하게 학습되는 반면 EO에서는 그런 혼동이 적어, 두 도메인의 특징 분포가 한 모델 안에서 충돌하는 것이 근본 원인이라고 분석했습니다.
B. IR 영상은 저해상도·저대비 특성 때문에 새·항공기의 열화상 실루엣이 UAV와 유사하게 학습되는 현상이 발생했습니다. 그에 비해 EO 카메라는
충분한 특징을 잘 잡을 수 있었고 EO/IR의 이미지 특성이 다르다고 판단하고 따로 weigths를 만드는것이 맞다고 생각이 들었습니다. 또한 항공기나 새같은 경우에는
굉장히 작은 객체로 탐지가 됩니다. 거의 인지하지 못하는 정도늰 30px 정도의 초소형 객체이도 하고 특히 새나 항공기 같은 경우 하늘과 비슷한 색깔일 경우에는 탐지가 
더 어려운 경우도 있었습니다.

**Q. 도메인별 모델 분리 시 리소스(GPU 메모리, 추론 지연) 오버헤드는 어떻게 관리했나요?**
A. 각 카메라 스트림이 EO 또는 IR 중 하나로 고정돼 있어, 런타임 도메인 판별 후 해당 모델로만 라우팅하면 됐습니다. 따라서 파드 하나가 두 모델을 동시에 들고 있을 필요가 없어 오버헤드는 파드 수(6개) 단위로 자연스럽게 분산됐습니다.

**Q. 고해상도 입력으로 인한 추론 속도 저하는 어떻게 관리했나요?**
A. 관심 영역(ROI) 위주 크롭과 다운샘플링 비율 최적화로 필요한 해상도만 모델에 전달해 실시간성과 탐지율 사이 균형을 맞췄습니다.

**Q. K8s 6파드 간 GPU 자원 경쟁은 어떻게 격리했나요?**
A. K8s 리소스 제한(requests/limits) 설정으로 파드별 GPU 메모리·CPU 사용량 상한을 지정해 특정 파드가 자원을 과점하지 못하도록 격리했습니다.

**Q. 탐지 결과를 연계한 추적 알고리즘은 무엇이었고, 프레임 드랍 시 재탐지는 어떻게 처리했나요?**
A. 형태가 단순한 UAV 특성상 경량 IoU 기반 tracker(프레임 간 bbox IoU로 동일 객체 매칭)를 사용해 별도 딥러닝 re-ID 모델 없이 연속 추적했습니다. 프레임 드랍 시 일정 프레임 이내 재탐지되면 동일 track ID를 유지하는 방식으로 완화했습니다.

**Q. 군보안 프로젝트의 보안 요구사항(망분리, 접근통제)이 설계에 준 제약은?**
A. 망분리 환경에서 동작해야 해서 외부 인터넷 연결 없이 모든 모델·의존성을 내부망에서 사전 패키징한 Docker 이미지로 배포했고, 결과 이미지를 공개할 수 없어 로그·산출물 반출도 별도 승인 절차를 따랐습니다.

---

## 4. 핫딜 자동화 시스템 (무무즈)

**Q. Google Spreadsheet 동시 수정 충돌과 Sheets API rate limit은 어떻게 관리했나요?**
A. 스프레드시트를 동시에 수정하는 주체는 배치 프로세스 하나였고, 사람의 실시간 편집과 겹치는 시점은 신청 마감 이후로 운영 규칙을 정해 회피했습니다. rate limit은 배치를 순차 호출하고 필요한 범위를 batchGet/batchUpdate로 묶어 호출 횟수를 최소화했습니다.
B. 사람이 직접 수정하는 서비스는 아예 없도록 전체 자동화가 되어있느 시스템으로진행하였습니다. 그래서 동시에 수정하는 충돌은 없었고요 api도 일주일간 신청구간과 상품 수집등 시간과 날짜가 다르기 때문에 rate limit은 없었습니다.

**Q. "당회차 실제 등록 상품 비율 기반 동적 가중치 스코어링"은 구체적으로 어떤 로직인가요?**
A. 할인율·가격 경쟁력·재고 수량 세 지표를 정규화한 뒤, 당회차 실제로 등록된 상품들의 지표 분포를 기준으로 가중치를 동적으로 재산정했습니다. 고정 가중치가 아니라 회차마다 "실제로 경쟁력 있는 상품의 특성"에 맞춰 가중치가 조정되는 방식입니다.

B. 현재 할인율, 단독할인가 유뮤, 신청 날짜, 재고수량, 할인율 차액등의 지표들을 가지고 정규화한뒤에 상품들의 지표로 핫딜 상품 200개을 선정했습니다.
또한 해당 브랜드에가 핫딜 상품으로 등록한 비율도 추가적으로 계산하고 상위노출 순위 노출 랭킹으로 로직을 구현하였습니다.

**Q. 별도 DB 없이 Spreadsheet로 완결한 구조의 확장성 한계는?**
A. 데이터 양이 커지거나 회차 간 이력 조회·통계 분석이 잦아지면 Spreadsheet는 쿼리 성능과 동시 접근 제어에 한계가 있어, 그 시점에는 RDB로 마이그레이션하는 것이 맞다고 생각합니다. 다만 초기에는 별도 인프라 비용 없이 빠르게 자동화를 구현하는 것이 우선순위였습니다.
B. 핫딜 서비스는 스프레드 시트도 사용하고 빅쿼리 DB도 함께 사용하여 브랜드 정보 및 분석데이터들과 로그들도 함께 저장하는 로직이였습니다.
구글 스프레드 시트는 브랜드별 상품 모집 및 신청, 상품 랭킹 순위, 각 브랜드별로 선정된 핫딜 상품도 자동으로 발송해주어야 했기 때문에 스프레드시트와 구글 지메일을
코드로 개발하여 자동화하는 로직을 잤습니다. 

**Q. Refresh Token이 revoke되거나 배치 중 인증이 끊기면 어떻게 감지·복구했나요?**
A. 배치 실행 전 토큰 유효성을 체크하는 단계를 두고, 실패 시 Slack Webhook으로 즉시 알림이 가도록 구성해 담당자가 빠르게 재인증할 수 있게 했습니다.
B. Google Sheets와 Google Drive를 사용하는 배치 작업은 OAuth 기반으로 Access Token과 Refresh Token을 함께 관리했습니다.
배치가 시작되기 전에 먼저 토큰의 유효성(creds.valid, creds.expired)을 확인하도록 구현했고, Access Token이 만료된 경우에는 Refresh Token을 이용해 새로운 Access Token을 자동으로 재발급받은 뒤 작업을 이어가도록 했습니다. 따라서 일반적인 토큰 만료로 인해 배치가 중단되는 상황은 발생하지 않도록 설계했습니다. 다만 Refresh Token 자체가 무효화되거나 OAuth 권한(Scope)이 변경된 경우에는 자동 복구가 불가능하기 때문에, 이 경우에는 사용자가 다시 OAuth 인증을 수행해 새로운 토큰을 발급받아야 하는 구조였습니다. 한편, 핫딜 서비스의 메인 데이터 소스인 Shopby API는 OAuth가 아닌 API Key 기반 인증을 사용했기 때문에, 인증관련 이슈가 발생하지 않도록 설계했습니다.

**Q. MIMEMultipart 기반 메일 모듈을 직접 구현한 이유는?**
A. 서드파티 이메일 서비스는 파트너사별 개인화 템플릿·이미지 첨부·BCC 대량 발송 요구에 맞추려면 추가 비용·연동 공수가 들었고, 이미 Gmail API를 OAuth로 인증해 쓰고 있어 직접 구현이 더 빠르고 비용 효율적이었습니다.
B. 메일로 보내는 형식과 브랜드별로 보내야 하는 고정 템플릿과 핫딜 상품 선정 리스트의 첨부파일 등 회사에서 사용하는 커스텀 메일 형식들이 따로 있었기 때문에
MIMEMultipart 기반으로 직접 만들었습니다. 차주 핫딜 신청 메일, 핫딜 상품 선정 메일, 상품미선정 메일, 핫딜 광고 메일, 전날 상품 알람 재고 확인 메일 등이 필요했기 때문에 핫딜 기반으로 직접 만들었습니다. 
**Q. Gmail API 발송 제한(quota)에 걸리지는 않았나요?**
A. 파트너사 수(수십 개 계정) 규모에서는 일일 한도 내 여유가 있었고, 필요 시 발송 간 약간의 딜레이를 둬 초당 호출 제한을 피했습니다.

**Q. 배포 실패 롤백 전략과 cron-배포 레이스는 어떻게 막았나요?**
A. 배포 실패 시 이전 커밋의 배포 아티팩트로 수동 재배포했고, cron과 배포가 겹치지 않도록 배포는 새벽 배치 시간대를 피해 스케줄링했습니다.

---

## 5. CS 문의 자동화 시스템 (무무즈)

**Q. Shopby·Sellmate 두 시스템 간 데이터 정합성이 어긋나는 경우는 어떻게 처리했나요?**
A. 두 API 응답에서 필요한 필드(송장번호, 입고예정일)가 모두 유효한 경우에만 자동 응답을 생성했고, 한쪽이라도 최신화되지 않은 것으로 보이는 이상치(과거 날짜 등)는 기본 배송 안내로 fallback 처리해 정합성 불일치로 인한 오답을 최소화했습니다.

**Q. 자동 처리 가능/불가 경계 케이스 오판 가능성은 어떻게 테스트·검증했나요?**
A. 실제 과거 문의 데이터를 케이스별로 분류해 회귀 테스트셋을 만들어 배포 전 오프라인 검증했고, 운영 초기에는 자동 응답 결과 일부를 담당자가 샘플링해 검수하는 기간을 뒀습니다.

**Q. 입고예정일 과거 날짜 이상치는 얼마나 자주 발생했고 원인은?**
A. 재고 시스템 갱신 지연으로 간헐적으로 발생했고, 자동 감지 후 기본 배송 안내로 fallback 처리해 오답 발송 리스크를 제거했습니다.
또한 재고 업데이트는 서비스상 회사에서 컨트롤할수있는것이 아니기 때문에 주기적으로 업데이트가 되었는지 확인하기 위해서 스케줄러로 한시간에 한번씩 답변하도록 구현했습니다.

**Q. 1시간 주기 cron 배치를 선택한 이유는? 웹훅과 비교한 트레이드오프는?**
A. 배송 문의는 실시간성보다 "몇 시간 내 응답"이면 충분한 업무였고, Shopby·Sellmate 모두 안정적인 웹훅을 지원하지 않아 웹훅은 구현 복잡도와 이벤트 유실 리스크가 더 컸습니다. 1시간 주기 cron은 구현이 단순하면서도 충분한 응답 속도를 낼 수 있었고, 핫딜처럼 즉시성이 중요한 업무가 아니라 웹훅의 재시도·순서 보장 같은 복잡도를 감수할 필요가 없었습니다.


**Q. API 인증 정보(config)의 secret 관리는 어떻게 했나요?**
A. JSON config는 `.gitignore`로 버전관리에서 제외하고 배포 서버에만 배치했으며, Bitbucket Pipelines의 secured 환경변수로 관리해 레포에 실제 값이 노출되지 않도록 했습니다.

---

## 6. 이약머약 — AI 알약 식별 서비스

**Q. Azure Custom Vision을 자체 모델 대신 선택한 이유와 트레이드오프는?**
A. 2주라는 짧은 기간 안에 학습·평가·배포를 끝내야 해서, 인프라 구축과 튜닝 시간을 아끼고 데이터 품질·서비스 완성도에 집중하기 위해 Custom Vision을 선택했습니다. 커스텀 CNN은 경량화·on-device 배포처럼 세밀한 제어가 필요할 때 유리하지만, 서버 기반 서비스에 짧은 기간 내 실용적 정확도를 확보하는 것이 우선이라 관리형 서비스의 개발 속도 이점이 더 컸습니다.

**Q. 90종 외 알약이 들어오면 어떻게 처리되나요?**
A. 예측 confidence가 임계치 이하인 경우 "식별 실패"로 안내해 임의로 엉뚱한 알약 정보를 확정 제공하지 않도록 처리했습니다.

**Q. 자동 크롤링에서 어떤 오염이 발생했고 왜 수동으로 전환했나요?**
A. 자동 크롤링은 검색 결과에 단종·재포장된 구형 디자인 이미지가 섞여 들어와, 실제 유통 중인 정제와 라벨이 다른데도 같은 클래스로 묶이는 오염이 발생했습니다. 완전 자동 필터링은 어려워 최신 유통 이미지인지 수동으로 확인하며 큐레이션하는 방식으로 전환했습니다.

**Q. augmentation 조정(회전 범위 축소 등) 전후 성능은 어떻게 비교했나요?**
A. 조정 전/후 동일 검증셋으로 클래스별 Recall을 비교했고, 각인·형태가 과도하게 왜곡된 학습 이미지가 오히려 알약의 미세한 특징 학습을 방해한다는 것을 확인한 뒤 조정을 확정했습니다.

**Q. 실사용 환경(손바닥, 탁자 등) 커버와 필드 테스트는 어떻게 진행했나요?**
A. 팀원들이 직접 다양한 배경·조명에서 촬영한 이미지로 자체 테스트를 진행해 배경 제거 전처리 효과를 확인했습니다. 약국 단위 정식 필드 테스트까지는 2주 내 진행하지 못했고, 향후 파일럿 도입 계획으로 남겨뒀습니다.

**Q. 색상·모양이 유사한 알약 간 Confusion은 없었나요?**
A. 동일 제조사의 유사 정제는 각인이 거의 유일한 구분 요소였는데, 각인이 흐릿한 경우 혼동되는 클래스 쌍이 있어 Recall 기준치 미달 취약 클래스로 분류해 맞춤 증강을 추가 적용했습니다.

**Q. 평균 응답 2.4초의 병목은 어디였나요?**
A. 병목은 Custom Vision 예측 API의 네트워크 왕복 시간이었고, 이미지 전처리(리사이즈)는 상대적으로 가벼워 미미했습니다. 이미지가 매번 달라 캐싱 효과가 제한적이라 별도 캐싱보다는 비동기 처리로 체감 대기시간을 최소화했습니다.

**Q. 오분류로 인한 안전 리스크는 어떻게 완화했나요?**
A. confidence가 낮은 경우 결과를 단정적으로 보여주지 않고 "가능성이 있는 후보"로 표현했고, 실제 복약 결정은 약사·의사 확인을 거치라는 안내 문구를 명시해 리스크를 완화했습니다.

---

## 7. PillCare — AI 기반 건강 위험 분석 서비스

### FastMCP / MCP

**Q. MCP를 도입한 이유와 REST API 대비 장점은?**
A. REST API로 제공하면 Agent가 각 엔드포인트 스펙을 프롬프트에 일일이 설명해야 하지만, MCP Tool은 표준화된 방식으로 입출력 스키마를 노출해 Agent가 스스로 언제 어떤 Tool을 호출할지 판단하게 할 수 있어 통합 비용이 낮았습니다.

**Q. Tool 간 의존성(약물 안전도가 건강 지수를 참조)은 어떻게 처리했나요?**
A. 약물 안전도 Tool 내부에서 필요한 건강 지수 값을 직접 조회하도록 구현해, Tool 간 직접 호출보다는 서비스 레이어에서 공통 데이터 접근 로직을 공유하는 방식으로 처리했습니다.

**Q. Tool 호출 순서·중복 호출을 제어하는 가드레일이 있었나요?**
A. Tool description에 명확한 호출 트리거 조건을 명시해 Agent의 판단을 유도했고, 동일 세션 내 중복 호출은 L1 캐시로 자연스럽게 흡수되도록 설계했습니다.

### 캐시 설계

**Q. L1이 여러 서버 인스턴스로 스케일아웃될 경우 캐시 불일치는 어떻게 처리하나요?**
A. 이 프로젝트는 단일 인스턴스로 운영해 실제 문제가 되지 않았습니다. 다중 인스턴스로 확장한다면 L1은 인스턴스 로컬 캐시로 유지하되 L2(Redis)를 신뢰 가능한 소스로 삼는 구조로 가야 한다고 생각합니다.

**Q. "L2 히트 시 L1 자동 워밍업"에서 cache stampede는 없었나요?**
A. 워밍업은 Redis에서 가져온 동일 값을 L1에 쓰는 연산이라 중복 LLM 호출로 이어지지 않았습니다. L1이 완전히 비어있는 콜드 스타트 시점에 다수 요청이 동시에 L3까지 도달할 가능성은 이론상 있었지만, 요청 빈도상 실제로는 드물었습니다.

**Q. Redis 장애 시 L3 폴백으로 인한 비용 급증은 어떻게 관리했나요?**
A. Redis 장애가 길어지면 L3 직접 호출이 늘어 비용이 증가할 리스크가 있었습니다. 이 단계에서는 별도 서킷브레이커까지는 두지 않았지만, 장애를 로그로 감지해 알림이 남도록 했습니다.

**Q. 캐시 키 설계 기준과 데이터 갱신 시 무효화는 어떻게 트리거되나요?**
A. 사용자 ID와 조회 시점(일 단위)을 조합해 키를 설계했고, 명시적 무효화보다는 L1 6시간 TTL·L2 자정 초기화로 최신 데이터가 자연스럽게 재계산되도록 설계했습니다.

### OCR / Vision

**Q. OCR 인식률이 낮을 때 사용자에게 어떻게 피드백했나요?**
A. 파싱 결과의 confidence가 낮으면 인식률에 대한 퍼센트로 함께 알려주고, 정확한것이 아닞 필수 필드(약품명 등)가 비어있으면 재촬영을 요청하는 안내를 제공하도록 설계했습니다.

**Q. LLM이 스키마를 어긴 응답을 줬을 때 재시도/폴백 전략은?**
A. LLM이 JSON 스키마를 제대로 반환하지 않거나 파싱에 실패하는 경우가 간혹 발생할 수 있습니다. 이때 사용자에게 에러를 그대로 보여주기보다는 우선 1회만 재시도하고, 그래도 실패하면 도메인별로 준비해둔 fallback 로직을 사용했습니다.

### 도메인/아키텍처

**Q. 도메인 경계는 어떤 기준으로 나눴고, 도메인 간 트랜잭션은 어떻게 처리했나요?**
A. 복약·건강지표·알람·사용자·처방 등 비즈니스 개념 단위로 도메인을 나눴고, 도메인 간 트랜잭션이 필요한 경우(복약 기록 저장 후 알람 갱신)는 서비스 레이어에서 각 도메인의 repository를 순차 호출하는 애플리케이션 트랜잭션으로 처리했습니다.

**Q. asyncio 스케줄러가 여러 인스턴스로 스케일아웃되면 중복 알람 발송 문제는 없나요?**
A. 맞습니다, 인메모리 세트는 단일 프로세스 내에서만 유효합니다. 이 프로젝트는 단일 인스턴스로 운영해 문제가 없었지만, 다중 인스턴스로 확장하려면 중복 차단 로직을 Redis 같은 공유 저장소 기반으로 옮겨야 한다고 인지하고 있습니다.

**Q. pytest로 OpenAI·Redis 같은 외부 의존성은 어떻게 격리했나요?**
A. pytest fixture와 mock/monkeypatch로 실제 호출을 대체해 정상·예외 응답을 시뮬레이션하며 도메인 로직만 격리해 검증했습니다.

---

## 8. 쀼라인드 — 부부·연인 관계 케어 AI 웹앱

### RAG 설계

**Q. Azure AI Search가 기본 제공하는 기능과 직접 설계한 오케스트레이션 레이어의 경계는?**
A. 임베딩 생성·청킹·인덱싱·기본 하이브리드 서치는 Azure AI Search의 On Your Data 관리형 기능에 위임했습니다. 직접 설계한 부분은 그 위에서 "언제 검색을 트리거할지", "in_scope를 얼마나 엄격히 적용할지", "검색 실패 시 어떤 응답을 낼지"를 응답 유형별로 분기하는 애플리케이션 정책 레이어였습니다. 핵심
AI를 사용할때 프롬프트를 전체적으로 컨트롤했고, 입력값과 출력값 예시, 가능한 조건들에 대한 것들을 상세히 작성했습니다.

**Q. 한 메시지에 일반 상담과 법률 질문이 섞인 경우 라우팅은 어떻게 했나요?**
A. 키워드 트리거가 법률 관련 표현을 감지하면 우선 RAG 경로로 라우팅해 근거 기반 응답을 우선시하고, 응답에서 일반 상담 부분은 별도 공감형 멘트로 보완했습니다. 완벽한 분리보다는 안전한 쪽(법률 정보 오류 방지)을 우선하는 방향으로 설계했습니다.

**Q. 키워드 기반 검색 트리거의 한계는 어떻게 보완했나요?**
A. 표현이 달라지면 놓칠 수 있다는 한계가 명확히 있었고, MVP 단계에서는 실제 사용자 발화 패턴을 수집해 트리거 키워드 목록을 계속 확장하는 방식으로 보완했습니다. 별도 의도 분류 모델 학습은 추후 과제로 남겼습니다.

**Q. RAG 실패 시 fallback이 트리거되는 조건은 구체적으로 무엇인가요?**
A. Azure AI Search의 in_scope 판별이 False로 나오거나 검색된 근거 문서의 관련성 점수가 낮은 경우를 조건으로 삼아, "정보 제공 범위를 벗어난다"는 안전한 답변과 공식 상담기관 연결 안내로 전환하도록 설계했습니다.

**Q. 판례·법령 데이터의 최신성은 어떻게 유지하나요?**
A. MVP 단계에서는 최근 20년 판례·법령을 스냅샷으로 인덱싱했고, 실서비스로 간다면 주기적으로(예: 분기별) 최신 판례를 재크롤링해 인덱스를 갱신하는 배치가 필요하다고 설계 문서에 명시했습니다.

### pgvector

**Q. 전용 벡터 DB 대신 pgvector를 선택한 이유와 대규모 확장 전략은?**
A. 이미 PostgreSQL을 메인 DB로 쓰고 있어 별도 인프라·비용 없이 확장 설치만으로 벡터 검색을 추가할 수 있다는 점이 5인 팀, 짧은 기간의 MVP에 적합했습니다. 수백만 벡터 규모가 되면 HNSW 인덱스 전환이나 전용 벡터 DB 분리를 고려해야 하지만, MVP 규모에서는 IVFFlat 또는 순차 스캔으로 충분했습니다.

**Q. 중복 판별 임계치(80%, 5분)는 어떻게 산정했나요? 오탐/미탐 사례는?**
A. 80%/5분은 데이터 기반 튜닝이 아니라 초기 구현 시 한 번 정한 값으로, 이후 변경 없이 그대로 사용했습니다. difflib.SequenceMatcher가 의미가 아닌 문자열 형태 유사도라, 표현이 다른 같은 사건은 놓치고(미탐) 반대로 짧은 문장은 앞부분만 겹쳐도 유사도가 과대평가될 수 있다는(오탐) 구조적 한계를 인지하고 있었지만, 실측 오탐/미탐을 모니터링하는 로직은 없었습니다.

**Q. 임베딩 모델과 pgvector 인덱스 타입은 무엇으로 설정했나요?**
A. Azure OpenAI의 text-embedding 계열 모델을 사용했고, pgvector 컬럼은 해당 차원에 맞춰 vector 타입으로 정의했습니다. MVP 규모에서는 정확도를 우선해 기본 인덱스로 충분했습니다.

### LLM 구조화 분석

**Q. JSON Schema를 어기는 응답 비율과 재시도 로직은?**
A. 초기 프롬프트만으로는 일부 응답에서 enum 외 값·필드 누락이 발생했는데, 구조화 출력 기능으로 스키마를 강제한 이후 위반 비율이 크게 낮아졌고, 남은 경우는 파싱 실패 시 동일 요청을 1회 재시도하는 로직으로 처리했습니다.

**Q. Gottman 패턴·PHQ-9를 라벨 체계에 매핑할 때 전문가 검증을 거쳤나요?**
A. 공개된 학술 자료를 참고해 팀 자체적으로 매핑했고, 실제 임상심리사 검증은 받지 못했습니다. 이는 MVP의 한계로 인지하고 있고, 상용화 단계에서는 전문가 자문이 필요하다고 로드맵에 명시했습니다.

**Q. 위험 신호 탐지 Recall 90.3%/F1 91.5%는 어떤 데이터셋으로 측정했나요?**
A. 팀이 위험 신호 포함/미포함 대화 예시를 직접 작성·라벨링한 테스트셋으로 측정했습니다. 실사용 데이터가 아닌 시나리오 기반 테스트셋이라는 한계가 있어, 실사용 데이터 기반 검증은 상용화 단계의 과제로 남겼습니다.

**Q. 위험 신호를 놓치는(FN) 경우의 안전장치는?**
A. 자동화된 판단에만 의존하지 않도록 자가점검 문항과 상담기관 CTA를 상시 노출하는 이중 안전장치를 뒀고, 관리자 콘솔에서 익명화된 위험 플래그를 사람이 검토할 수 있는 여지를 남겼습니다.

### 개인정보 / 안전성

**Q. "관계어는 보존"하는 마스킹 규칙은 어떻게 구현했나요?**
A. Azure AI Language의 기본 PII 탐지가 "남편", "아내" 같은 관계 지칭어를 사람 이름으로 오탐지하는 경우가 있어, 탐지된 개체 중 사전에 정의한 관계어 화이트리스트에 해당하면 마스킹 대상에서 제외하는 후처리 규칙을 추가로 구현했습니다.

**Q. 채팅 캡처 이미지의 보관 기간·암호화·접근 통제는 어떻게 설계했나요?**
A. Blob Storage 저장 시 Azure 기본 저장소 암호화(at-rest)를 적용했고 접근은 서버 측 자격증명으로만 제한했습니다. 보관 기간 자동 삭제 정책은 MVP 단계에서 완전히 구현하지 못해 상용화 로드맵 과제로 남겼습니다.

### 팀/운영

**Q. 프론트엔드/다른 백엔드 담당자와 API 계약은 어떻게 관리했나요?**
A. api 명세서를 작성하고, QA 1/2/3차 등 여러번 기능적으로 테스트까지 해보면서 명세서로 기록하고 관리했습니다.
**Q. 관리자 콘솔의 "익명 요약"은 어떤 로직으로 생성했나요?**
A. 원문 대신 이미 만들어진 LLM 구조화 분석 결과(감정 라벨, 위험 플래그)만 노출해, 별도의 요약 생성 단계 없이도 구조화된 라벨 자체가 익명 요약 역할을 하도록 설계했습니다.

**Q. 예산 60만 원 중 10만 원만 쓸 수 있었던 비용 최적화 포인트는?**
A. 멀티 레이어 캐시로 반복 요청의 LLM 재호출을 줄인 것과, 임베딩·구조화 분석에 상대적으로 저렴한 모델을 사용한 것이 핵심이었습니다.

---

## 9. 공통 ML/컴퓨터비전 개념 질문

**Q. mAP, Precision, Recall, F1-score, IoU의 정의와 관계는?**
A. IoU는 예측 박스와 정답 박스의 겹침 정도, Precision은 예측한 것 중 맞은 비율, Recall은 실제 정답 중 맞춘 비율, F1은 둘의 조화평균, mAP는 여러 클래스·IoU threshold에 걸친 평균 정밀도입니다. 오탐 리스크가 큰 드론 프로젝트는 Precision을, 놓치면 안 되는 위험 신호 탐지(쀼라인드)는 Recall을 우선했습니다.

**Q. Precision과 Recall 중 하나를 포기해야 한다면 기준은?**
A. 오탐(FP)의 비용이 미탐(FN)의 비용보다 크면 Precision을, 반대면 Recall을 우선합니다.  또한 프로젝트나 서비스에 따라서도 달라진다고 생각합니다.
드론 프로젝트는 오인식이 현장 대응 오류로 이어져 Precision을, 쀼라인드의 위험 신호 탐지는 놓치는 게 더 위험해 Recall을 우선했습니다.

**Q. Faster R-CNN(2-stage)과 YOLO(1-stage)의 구조적 차이는?**
A. Faster R-CNN은 region proposal 이후 분류·회귀를 수행하는 2-stage라 정확도가 높지만 느리고, YOLO는 이미지 전체를 grid 단위로 한 번에 예측하는 1-stage라 빠르지만 작거나 밀집한 객체에서 상대적으로 정확도가 떨어지는 경향이 있습니다.

**Q. "소형 객체 특화 앵커"는 구체적으로 무엇을 조정하는 것인가요?**
A. 앵커의 크기(scale)를 작은 객체에 맞게 조정하고, 저해상도 feature map보다 고해상도 FPN 레벨에서 소형 객체를 예측하도록 배치를 조정하는 것을 의미합니다.

**Q. 클래스 불균형 문제에 어떤 기법을 사용했나요?**
A. Augmentation(회전·밝기·노이즈), 합성 데이터 생성, hard negative mining, 필요 시 focal loss 등을 프로젝트 상황에 맞게 사용했습니다.

**Q. 도메인 갭은 무엇이고 프로젝트별로 어떻게 완화했나요?**
A. 학습 데이터와 실제 배포 환경 데이터의 분포 차이를 의미하며, 드론 프로젝트는 실환경 촬영+합성 데이터 확장으로, 이약머약은 배경 제거+Background Augmentation으로 각각 완화했습니다.

**Q. 전이학습과 초기학습(from scratch)의 선택 기준은?**
A. 사전학습 가중치가 도메인과 유사한 특징을 이미 학습했다고 판단되면 전이학습을 기본으로 하고, 데이터가 충분히 많고 도메인이 매우 특수(위성·열화상)한 경우 초기학습 또는 도메인 특화 사전학습을 함께 검토했습니다.

**Q. ONNX 변환 시 자주 겪는 이슈는?**
A. NMS 같은 커스텀 연산자 미지원, 동적 batch/입력 크기 처리, opset 버전 차이로 인한 연산자 미스매치가 자주 겪는 이슈였습니다.

**Q. MLflow로 어떤 정보를 트래킹하고 재현성을 어떻게 확보했나요?**
A. 학습 파라미터, loss curve, 성능 지표, 모델 아티팩트 경로를 트래킹해 동일 config로 재실행 시 이전 결과와 비교할 수 있도록 재현성을 확보했습니다.

**Q. 라벨링 품질(inter-annotator agreement)은 어떻게 관리했나요?**
A. 라벨링 가이드라인을 문서화하고 팀 내 샘플 교차 검수를 통해 occlusion 처리 등 라벨링 기준을 통일했습니다.

---

## 10. 공통 LLM/RAG 개념 질문

**Q. RAG의 기본 파이프라인과 흔한 실패 유형은?**
A. 청킹→임베딩→검색→생성 순서로 동작하며, 청킹 크기가 부적절하면 검색 정확도가 떨어지고, 임베딩과 질의 의미가 어긋나면 관련 없는 문서가 검색되며, 생성 단계에서 근거 문서를 무시하고 hallucination이 발생하는 것이 흔한 실패 유형입니다.

**Q. few-shot과 구조화 출력(JSON Schema/function calling)의 장단점은?**
A. few-shot은 형식을 유도하지만 여전히 자유 텍스트라 파싱 실패 리스크가 있는 반면, 구조화 출력은 모델이 스키마를 따르도록 강제해 파싱 안정성이 훨씬 높습니다. 다만 복잡한 추론이 필요한 경우 few-shot이 더 유연한 사고 과정을 유도할 수 있습니다.

**Q. LangChain LCEL의 장점은?**
A. 체인을 선언적으로 조합할 수 있어 스트리밍·비동기·재시도 같은 공통 기능을 체인 전체에 일관되게 적용할 수 있다는 장점이 있습니다.

**Q. Hallucination을 줄이기 위해 적용한 기법은?**
A. RAG 근거 문서를 명시적으로 인용하도록 프롬프트를 설계하고, in_scope 판별로 근거가 부족하면 답변을 제한하는 fallback을 두는 방식으로 완화했습니다.

**Q. Vector DB 선택 시 고려 요소와 pgvector 대비 전용 벡터 DB의 트레이드오프는?**
A. 데이터 규모, 레이턴시, 기존 인프라와의 통합 용이성, 하이브리드 서치 지원 여부를 고려합니다. pgvector는 기존 RDB에 자연스럽게 통합되는 게 장점이고, 전용 벡터 DB는 대규모·고성능 검색에 특화돼 있다는 차이가 있습니다.

**Q. LLM 비용 절감 전략은?**
A. 멀티 레이어 캐싱, 반복 요청 결과 재사용, 태스크 난이도에 맞는 모델 선택, 프롬프트 토큰 최적화를 실무에서 적용했습니다.

**Q. Azure OpenAI와 OpenAI(공식 API)의 차이, Azure를 선택한 이유는?**
A. Azure OpenAI는 기업 보안·컴플라이언스 요구사항(데이터 리전, 접근 통제)을 충족하기 쉽고 기존 Azure 인프라와 통합이 쉬워, Azure AI Search·AI Language 등을 함께 쓰는 프로젝트에서 선택했습니다.

---

## 11. 백엔드/인프라 공통 질문

**Q. FastAPI async는 언제 쓰고, blocking I/O를 async 안에서 호출하면 어떤 문제가 생기나요?**
A. 외부 API 호출·DB 쿼리 같은 I/O 바운드 작업은 async로 처리해 동시성을 높였습니다. blocking 라이브러리를 async 함수 안에서 그대로 호출하면 이벤트 루프 전체가 블로킹되어 다른 요청 처리가 지연되므로, 이런 경우는 별도 스레드풀로 위임했습니다.

**Q. Docker/K8s로 로컬-운영 환경 일관성을 어떻게 보장했나요?**
A. 동일한 Docker 이미지를 로컬 개발과 운영 배포에 동일하게 사용해 환경 차이를 없앴습니다.

**Q. Redis를 캐시·큐 용도로 함께 쓸 때 리소스 경합은 어떻게 대응했나요?**
A. 같은 인스턴스에서 캐시와 큐를 함께 쓰면 메모리 경합·evict 정책 충돌이 생길 수 있어, 용도별 키 네임스페이스와 TTL 정책을 분리해 관리했고, 규모가 커지면 인스턴스를 분리하는 것이 맞다고 생각합니다.

**Q. pgvector 추가가 기존 트랜잭션 성능에 영향을 줬나요?**
A. 확장(extension) 형태로 추가돼도 기존 트랜잭션 처리에는 큰 영향이 없었고, 벡터 검색 쿼리 자체의 성능은 데이터 규모에 따라 인덱스 전략을 조정해야 한다는 점을 염두에 뒀습니다.

**Q. CI/CD에서 배포 실패는 어떻게 감지·롤백했나요?**
A. Bitbucket Pipelines에서 테스트 실패 시 배포 단계가 진행되지 않도록 구성했고, 배포 후 문제 발생 시에는 이전 태그로 재배포하는 수동 롤백 절차를 뒀습니다.

**Q. Cron과 웹훅 중 선택 기준, 핫딜·CS 자동화 비교는?**
A. 핫딜은 명확한 배치 타이밍(신청 마감 후 일괄 처리)이 있어 cron이 자연스러웠고, CS 자동화도 실시간성보다 주기 처리로 충분해 cron을 택했습니다. 웹훅은 실시간성이 중요하고 상대 시스템이 이벤트를 안정적으로 보장할 때 유리하지만, 두 프로젝트 모두 그 정도 실시간성이 필요하지 않았고 웹훅의 재시도·순서 보장 복잡도를 감수할 필요가 없었습니다.

**Q. OAuth 2.0 Authorization Code/Refresh Token 흐름과 서버 간 무인 갱신은?**
A. Authorization Code Flow로 최초 1회 사용자 동의를 받아 Refresh Token을 발급받고, 이후 배치는 Refresh Token으로 Access Token을 자동 갱신해 사용자 개입 없이 인증을 유지했습니다.

---

## 12. 커리어/이력 관련 질문

**Q. 영상 AI → 이커머스 자동화 → LLM/RAG로 도메인을 옮기며 재사용한 역량은?**
A. 도메인은 바뀌었지만 "문제를 파이프라인으로 쪼개고, 병목을 찾아 자동화하고, 지표로 검증하는" 접근 방식은 항상 동일하게 적용했습니다.

**Q. 여러 프로젝트를 동시에 수행할 때 우선순위와 컨텍스트 스위칭 비용은 어떻게 관리했나요?**
A. 고객 임팩트(실사용에 임박한 프로젝트)와 마감 기한을 기준으로 우선순위를 정했고, 프로젝트 간 공통 기술 스택(PyTorch, Docker/K8s)을 공유해 컨텍스트 스위칭 비용을 줄였습니다.

**Q. 5인 팀 프로젝트에서 의견 충돌이 있었던 기술적 결정은 어떻게 합의했나요?**
A. pgvector 임계치나 RAG scope 엄격도처럼 정답이 정해지지 않은 설계 결정에서는 실제 시나리오를 재현해 데이터로 비교한 뒤 합의했습니다.

**Q. 짧은 기간의 팀 프로젝트에서 상용화 수준까지 설계한 이유는?**
A. MVP 시연에서 끝나면 실사용 가능성을 증명하지 못한다고 판단해, 짧은 기간이라도 안전성 지표·운영 콘솔처럼 실제 서비스화에 필요한 요소를 최소 단위로라도 설계해 넣는 것을 우선순위로 삼았습니다.

**Q. 군보안/공공기관 프로젝트와 스타트업 프로젝트의 가장 큰 차이는?**
A. 군보안/공공기관은 요구사항이 사전에 문서화된 스펙(KTL 시험 기준 등)으로 명확히 주어지는 반면, 스타트업은 요구사항 자체가 빠르게 바뀌고 실사용자 피드백을 즉시 반영해야 하는 속도감이 컸습니다.

**Q. "더 큰 규모의 ML 시스템 운영"이 구체적으로 어떤 지향인가요?**
A. 지금까지 쌓은 MLOps 플랫폼·파이프라인 자동화 경험을 살려, 더 많은 모델과 트래픽을 안정적으로 운영하는 ML 시스템/플랫폼 엔지니어링 역할로 확장하고 싶습니다.
