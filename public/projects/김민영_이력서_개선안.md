# 김민영

**ML / MLOps Engineer**

premierckim@gmail.com · github.com/my0614 · mykim.site · Seoul

---

## 모델 3개짜리 파이프라인을, 20개 플랫폼으로

모델마다 입력 포맷·학습 방식·출력 구조가 달라 신규 모델 추가에 1~2주씩 걸리던 구조를, GOD·MMDetection·MMYOLO 3개 프레임워크를 단일 ML 백엔드 인터페이스로 추상화해 **지원 모델 20개 이상, 신규 온보딩 1일**로 줄였습니다. DB 폴링 학습 큐를 Redis BRPOP 원자적 큐로 전환해 하루 이상 밀리던 학습 대기를 자동 순차 처리로 해소했고, nvidia-smi 기반 GPU 동적 분배로 멀티 GPU 환경의 OOM 장애를 제거했습니다.

## 모델은 현장에서 검증되어야 한다

공개 데이터셋이 존재하지 않는 동굴·터널 환경에서, 직접 현장에 들어가 촬영·라벨링하고 언리얼 엔진으로 3D 에셋을 제작해 합성 데이터를 만드는 것부터 시작했습니다. 이렇게 데이터를 2,000장에서 6,000~7,000장까지 3배 이상 확장해, 저조도·빛번짐에서 무너지던 드론 탑재 탐지 모델을 **mAP 61%→75%**까지 끌어올리고 KTL 시험 성적서를 받았습니다. EO/IR 도메인 분리 학습과 소형 객체 특화 최적화로 주야간 UAV 탐지를 안정화했고, K8s 6개 파드 RTSP 병렬 처리로 채널 장애가 전체로 번지지 않는 실시간 운영 구조를 만들었습니다. 이 밖에도 수계 탐지, 경지(농지) 탐지, 초소형군집위성 토지피복 분류, TCD·MCD 변화탐지까지 도메인을 바꿔가며 모델 학습을 이어왔습니다.

## 도메인이 바뀌어도, 자동화의 문법은 같았다

이커머스로 도메인을 옮겨서도 같은 방식으로 문제를 풀었습니다. 파트너사 신청부터 등록·메일 발송까지 **4시간 걸리던 핫딜 운영을 E2E 파이프라인으로 2분(99% 단축)**까지 줄여 무인화했고, Shopby·Sellmate 이중 API 매칭으로 **CS 문의 1/3을 자동 응답**으로 전환해 응답 지연을 80% 줄였습니다. 위성·드론에서 익힌 파이프라인 설계가 특정 도메인에 묶인 기술이 아님을 확인한 경험입니다.

이 경험을 더 큰 규모의 ML 시스템 운영으로 이어가고 싶습니다.

---

# 경력

## 한컴인스페이스 — 연구원 · 소프트웨어플랫폼사업팀

2021.10 ~ 2025.12 (4년 3개월) · AI 모델 학습·추론 파이프라인 고도화 및 위성·드론 영상 서비스 운영

### 사내 MLOps 플랫폼 DFLOW | 2022.01 ~ 2024.12

데이터 라벨링(오토라벨링)부터 학습 스케줄링, 학습 추적, 성능 평가, ONNX 추출까지 제공하는 통합 MLOps 플랫폼. 전 컴포넌트를 K8s 파드로 운영했습니다.

**통합 ML 백엔드 인터페이스**
- 원인: 모델별로 입력 포맷·학습 방식·출력 구조가 달라 신규 모델 추가마다 반복 재구현 필요
- 해결: GOD·MMDetection·MMYOLO 3개 프레임워크를 단일 ML 백엔드 인터페이스로 추상화
- 결과: 지원 모델 3개 → 20개 이상, 신규 온보딩 1~2주 → 1일

**오토라벨링 파이프라인**
- 원인: 위성·드론 영상 라벨링을 전량 수작업으로 수행해, 데이터셋 구축이 학습 사이클 전체의 병목
- 해결: 학습된 모델의 추론 결과를 사전 라벨(pre-label)로 자동 생성해 라벨링 도구에 연동, 작업자는 처음부터 그리는 대신 검수·수정만 수행하는 구조로 전환. 검수 완료 데이터가 다시 학습에 투입되는 라벨링-학습 선순환 루프 구성
- 결과: 라벨링 공수 대폭 절감, 모델이 좋아질수록 라벨링이 빨라지는 데이터 플라이휠 확보

**Redis BRPOP 기반 학습 작업 큐**
- 원인: DB 폴링 방식 학습 큐에서 race condition으로 다중 Pod 환경 순서 보장 불가
- 해결: Redis BRPOP 원자적 큐 + Redis Hash 상태 관리로 전환
- 결과: 하루 이상 걸리던 학습 대기를 자동 순차 처리로 해소

**GPU 자원 관리 — nvidia-smi 동적 분배에서 GPUShare 스케줄링으로**
- 원인: GPU 가용 메모리 확인 없이 학습을 배정해 멀티 GPU 환경에서 OOM 장애 반복
- 해결: 1차로 nvidia-smi로 가용 메모리를 실시간 확인해 모델 요구량과 비교하는 동적 분배 로직을 구현하고, 이후 Aliyun GPUShare를 도입해 GPU를 fraction 단위로 나눠 쓰는 K8s 스케줄러 레벨 자원 관리로 발전시킴. 학습 작업 자체도 Docker 컨테이너(K8s 파드) 단위로 실행해 환경 재현성과 격리를 확보
- 결과: 멀티 GPU OOM 장애 제거, 한정된 GPU 위에 더 많은 학습·추론 워크로드를 동시 배치할 수 있는 구조 확보

### 드론 탑재 실시간 객체 탐지 · 3D 가시화 | 2023.01 ~ 2025.12

전쟁 지역·붕괴 위험 터널·저조도 환경 등 접근이 어려운 지역을 원격 탐색하는 드론 기반 실시간 객체 탐지 시스템.

**데이터셋이 없는 환경: 직접 촬영·라벨링·언리얼 합성으로 만든 학습 데이터**
- 원인: 동굴·터널 내부 위협 객체는 공개 데이터셋 자체가 없었고, 초기 모델은 저조도·빛번짐 환경의 오탐·미탐으로 mAP 61%에 그침
- 해결: 실제 동굴에 들어가 직접 촬영하고 전량 라벨링을 수행. 실측으로 확보할 수 없는 환경·객체 조합은 언리얼 엔진으로 3D 에셋을 제작해 합성 이미지를 생성하고 실사 배경과 합성. 오탐 원인별로 데이터를 2,000장에서 6,000~7,000장까지 3배 이상 확장해 재학습
- 결과: mAP 61% → 75% (14%p 향상), KTL 시험 성적서 발급

**RGB/Depth 프레임 동기화**
- 원인: RGB/Depth ROS Topic 프레임 불일치로 실시간 거리 계산 정확도 저하
- 해결: ApproximateTimeSynchronizer의 slop·queue size를 실측 튜닝해 프레임 동기화
- 결과: 거리 오차 30~40% 감소

**네트워크 환경 자동 세팅**
- 원인: 드론·환경마다 네트워크 설정이 달라 수동 세팅에 2시간 이상 소요
- 해결: 서브넷 기반 환경 자동 판별과 IP 자동 세팅 로직 구현
- 결과: 세팅 시간 2시간 → 1분 이하 (99% 단축)

### EO/IR UAV 실시간 탐지·추적 시스템 | 2024.06 ~ 2024.12

EO·IR 이중 도메인 환경에서 소형 UAV를 실시간 탐지·추적하는 모니터링 시스템.

**EO/IR 도메인별 모델 분리 학습**
- 원인: EO·IR 이미지를 단일 데이터셋으로 학습해 IR 환경에서 새·항공기 오탐 빈번
- 해결: EO/IR 도메인별로 데이터셋을 분리해 독립 학습, 고해상도 입력과 소형 객체 특화 앵커·증강 적용
- 결과: 주야간 모두 안정적인 소형 UAV 탐지 성능 확보, 미탐 감소

**K8s 6파드 RTSP 병렬 처리**
- 원인: 단일 파드로 다중 카메라 스트림 처리 시 한 채널 장애가 전체에 영향
- 해결: K8s 6개 파드로 EO/IR 카메라별 RTSP 스트림을 독립 처리
- 결과: 6채널 실시간 병렬 처리, 장애 격리

### 그 외 모델 학습·서빙 경험

- **변화탐지 AI 플랫폼(APISS)** — 초소형군집위성 영상 기반 토지피복 세그멘테이션(나지·초지·농지·숲·수역·빌딩·도로) 및 20클래스 객체탐지 모델 학습. 객체탐지는 YOLO26을 우선 검토했으나 자체 데이터 벤치마크에서 성능이 기대에 못 미쳐 YOLO11m 채택 — 릴리스 순서가 아닌 실측 결과 기반으로 모델을 선정
- **[DNA] DeepUNet 기반 수계탐지 추론 서비스** (2023.07 ~ 2023.10) — 위성영상 수계 세그멘테이션 모델 학습과 Flask 기반 추론 API 서버 구현에 참여. 학습된 모델을 API로 감싸 외부 시스템과 연동하는 구조를 다룬 첫 경험으로, 이후 DFLOW ML 백엔드 인터페이스 설계의 기반이 됨 `Python` `PyTorch` `Flask` `Docker`
- **경지면적조사 시스템** — 위성영상 기반 농지(경지) 탐지 모델 학습 수행
- **국가 보안기관 위성영상 AI 플랫폼** — TCD·MCD 변화탐지 모델 학습 수행

## (주)무무즈 — 매니저 · P&C

2025.12 ~ 2026.03 (4개월) · 이커머스 운영 자동화 시스템 구축

### 핫딜 자동화 시스템

**핫딜 등록 E2E 자동화 파이프라인**
- 원인: 파트너사 신청부터 상품 선정·등록·결과 메일 발송까지 수작업으로 등록에만 4시간 이상 소요
- 해결: E2E 자동화 파이프라인 설계, Shopby API 연동 및 동적 스코어링으로 상위 200개 자동 선별
- 결과: 작업 시간 4시간 → 2분 (99% 단축), 전 과정 무인 자동화

**OAuth 2.0 통합 인증 및 무중단 배치 · CI/CD**
- 원인: Gmail·Sheets·Drive 3개 API 개별 인증으로 새벽 배치 중 인증 만료 위험, 배포는 수동 반영
- 해결: OAuth 2.0 단일 credential 통합 + Refresh Token 자동 재발급, Bitbucket Pipelines로 테스트~EC2 배포 자동화(dev/prod 분리)
- 결과: 무중단 배치 파이프라인 확보, 배포 리드타임 단축·배포 실수 리스크 제거

### CS 문의 자동화

**이중 API 기반 자동 응답 파이프라인**
- 원인: 반복 배송 문의를 상담원이 수동 처리, 주말·비업무 시간 대응 불가
- 해결: Shopby·Sellmate 이중 API 매칭으로 상품코드·송장번호 조합 3케이스 분기, 처리 불가 케이스는 담당자 플래그 분리, cron 배치 24/7 처리
- 결과: 문의의 1/3 자동 처리, 응답 지연 80% 단축, 오발송 리스크 제거

---

# 사이드 프로젝트

## 쀼라인드 — 부부·연인 관계 케어 AI 웹앱 | 2026.06 ~ 07

5인 팀 · DB/AI 분석 파이프라인 담당

**LangChain 기반 RAG 상담 챗봇**
- 원인: 가사소송법·판례 상담 답변에 근거가 없어 신뢰도가 낮음
- 해결: LangChain과 Azure AI Search로 법령·판례를 벡터 검색한 뒤 근거를 명시하는 RAG 파이프라인 설계
- 결과: 출처를 명시한 상담 응답 생성

**pgvector 기반 유사도 검색 · LLM 구조화 분석**
- 원인: 키워드 매칭으로는 유사 맥락 탐지 불가, 자유 문장 추출은 DB 집계 신뢰성 저하
- 해결: PostgreSQL pgvector 임베딩 유사도 검색 구현, 14개 그룹 81개 라벨 JSON Schema 강제 포맷으로 감정 분석 구조화
- 결과: 별도 벡터 DB 없이 추천·중복판별 동시 처리, 필드 누락 없이 안정적 DB 적재

## 이약머약 — AI 알약 식별 서비스 | 2026.05

사진 한 장으로 알약을 식별해 약물 정보·DUR 상호작용까지 제공하는 서비스 · Azure Custom Vision 기반 팀 프로젝트

**핵심 90종 분류 모델 구축**
- 원인: 전 성분·전 제형을 무분별하게 수집하면 클라우드 비용 부담과 데이터 오염 위험 증가
- 해결: 처방 통계·판매량 기반으로 핵심 90종을 선별, 학습 데이터 3,773개로 분류 모델 구축
- 결과: 성능지표 평균 97% 달성

**FastAPI 비동기 예측 연동 · 올인원 배포**
- 원인: 동기 처리로는 다수 요청 시 응답 지연 우려, 2주 내 기획부터 배포까지 완결해야 하는 제약
- 해결: Azure SDK 예측 API를 FastAPI 비동기 엔드포인트로 통합, Azure VM에 FastAPI·PostgreSQL·Nginx 올인원 배포
- 결과: AI 분석 평균 응답 2.4초, 2주 내 풀스택 완성

## 디얼미 | (기간 확인 필요)

*(프로젝트 개요, 담당 역할, 기술 스택, 핵심 성과를 알려주시면 채워 넣겠습니다)*

---

# 기술 스택

- **Language** Python · C
- **NLP/LLM** LLM · RAG · Azure OpenAI · LangChain · Prompt Engineering
- **Vision** YOLOv5 · Faster R-CNN · RetinaNet · MMDetection · MambaCD
- **AI Framework** PyTorch
- **Backend** FastAPI · Flask
- **DB** PostgreSQL · Redis · ChromaDB · pgvector
- **Infra/Cloud** Docker · Kubernetes · Azure · AWS EC2
- **Else** Git · Jira · Label Studio · MLflow

---

# 학력

- **고려사이버대학교** AI·데이터과학부 | 2022.02 ~ 2026.02 (졸업)
- **대덕소프트웨어마이스터고등학교** 임베디드SW과 | 2019.03 ~ 2022.02 (졸업)

# 자격증

- 데이터분석 준전문가(ADsP) · 한국데이터산업진흥원 · 2024.11
- 정보처리기능사 · 한국산업인력공단 · 2019.06

# 활동

- **PillCare — AI 기반 건강 위험 분석 서비스** (식의약 공공데이터·AI 분석·활용 경진대회, 2026.05) · FastMCP 서버로 건강 지수·약물 안전도·환경 지수 3개 Tool을 구현해 AI Agent가 실시간 데이터를 직접 조회하도록 연동, L1 인메모리·L2 Redis·L3 OpenAI 3단계 캐시로 호출 비용 절감과 Redis 장애 시 무중단 폴백 확보
- **마이크로소프트 AI School 10기** 수료 (2026)
